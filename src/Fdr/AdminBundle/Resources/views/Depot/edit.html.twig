{% extends '::base.html.twig' %}
{% block title %}Depot{% endblock %}
{% block body -%}
    <h1>Depot edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('depot') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
