{% extends '::base.html.twig' %}
{% block title %}Filiale|Accueil{% endblock %}
{% block body -%}
    <h1>Filiale list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Chauffeur</th>
                <th>Motif</th>
                <th>Date de but</th>
				<th>Date de fin</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('indisponibilite_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.chauffeur }}</td>
                <td>{{ entity.motif }}</td>
				<td>{{ entity.datedebut }}</td>
				<td>{{ entity.datefin }}</td>
                <td>
                        <div class="col-md-2">
                         <a href="{{ path('indisponibilite_show', { 'id': entity.id }) }}">
                         <button title="Afficher" type="submit">
                         <img id="inputButtonAfficher" title="Afficher" src="{{ asset('bundles/fdradmin/images/apercu.png') }}" />
                         </button>
                         </a> 
                        </div>
                        &nbsp;&nbsp;
                        
                        <div class="col-md-2">
                         <a href="{{ path('indisponibilite_edit', { 'id': entity.id }) }}">
                         <button title="Afficher" type="submit">
                         <img id="inputButtonModifier" title="Modifier" src="{{ asset('bundles/fdradmin/images/modifier.png') }}" />
                         </button>
                         </a> 
                        </div>
                        &nbsp;&nbsp;

                        <div class="col-md-2">
                        
                        {{ form_widget(form[entity.id]) }}
                        </div>
                        &nbsp;&nbsp;
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('indisponibilite_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
