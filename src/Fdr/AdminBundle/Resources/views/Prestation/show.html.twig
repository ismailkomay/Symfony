{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Prestation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ entity.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Champssupp1</th>
                <td>{{ entity.champssupp1 }}</td>
            </tr>
            <tr>
                <th>Champssupp2</th>
                <td>{{ entity.champssupp2 }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('prestation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('prestation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
