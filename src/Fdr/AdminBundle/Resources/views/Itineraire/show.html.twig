{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Itineraire</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Depart</th>
                <td>{{ entity.depart }}</td>
            </tr>
            <tr>
                <th>Arrivee</th>
                <td>{{ entity.arrivee }}</td>
            </tr>
            <tr>
                <th>Remarques</th>
                <td>{{ entity.remarques }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ entity.disponible }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('itineraire') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('itineraire_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
