{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BonCarburantHuile</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ entity.lieu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ entity.quantite }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ entity.montant }}</td>
            </tr>
            <tr>
                <th>Compteur</th>
                <td>{{ entity.compteur }}</td>
            </tr>
            <tr>
                <th>Champsup1</th>
                <td>{{ entity.champSup1 }}</td>
            </tr>
            <tr>
                <th>Champsup2</th>
                <td>{{ entity.champSup2 }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('boncarburanthuile') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('boncarburanthuile_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
