{% extends '::base.html.twig' %}
{% block title %}BonCarburantHuile|Accueil{% endblock %}
{% block body -%}
    <h1>BonCarburantHuile list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Lieu</th>
                <th>Date</th>
                <th>Quantite</th>
                <th>Montant</th>
                <th>Compteur</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('boncarburanthuile_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.lieu }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.quantite }}</td>
                <td>{{ entity.montant }}</td>
                <td>{{ entity.compteur }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('boncarburanthuile_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('boncarburanthuile_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('boncarburanthuile_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
